#include <gtest/gtest.h>
//#include "../SCXMLDesigner/scxmlstate.h"

/*
TEST(SCXMLStateTests, SetShapeXChangesValueCorrectly) {
    SCXMLState* state = new SCXMLState("state1");
    for (int i=0; i< 200; i++) {
        state->SetShapeX(i);
        EXPECT_EQ(i, state->GetShapeX());
    }
}

TEST(SCXMLStateTests, SetShapeYChangesValueCorrectly) {
    SCXMLState* state = new SCXMLState("state1");
    for (int i=0; i< 200; i++) {
        state->SetShapeY(i);
        EXPECT_EQ(i, state->GetShapeY());
    }
}
*/

